comment: false

fixes:
  - "/package/::"    # Since Dockerfile sets WORKDIR to /package

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        informational: true
    patch:
      default:
        target: auto
        threshold: 1%
        informational: true
